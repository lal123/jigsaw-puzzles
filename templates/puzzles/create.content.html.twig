{# /templates/puzzles/create.content.html.twig #}
        <div class="container">
            <h1>{% trans %}Create a puzzle{% endtrans %}</h1>
        </div>
        <div class="container">
            <div class="row">
                {{ form_start(form, {
                    'attr': {
                        'novalidate': 'novalidate',
                        'class': 'col-lg-5' 
                        }
                    })
                }}
                {{ form_end(form) }}
            </div>
        </div>
