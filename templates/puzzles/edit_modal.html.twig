{# /templates/puzzles/edit_modal.html.twig #}

{% block body %}
        <div class="container">
            <div class="row">
                {{ form_start(form, {
                    'attr': {
                        'novalidate': 'novalidate',
                        'class': 'col-lg-5'
                        }
                    })
                }}
                {{ form_end(form) }}
            </div>
        </div>
{% endblock %}
