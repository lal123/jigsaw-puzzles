<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            {{ encore_entry_link_tags('bst') }}
        {% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <img src="{{ asset('build/img/logo.png') }}">
        {% block menu %}
        	  <a href="{{ path('app_puzzles_list') }}">Puzzles List</a>
        	| <a href="{{ path('app_user_create_account') }}">Create an account</a>
        {% endblock %}
        {% block body %}{% endblock %}
		{% block editPuzzle %}{%endblock%}
    </body>
</html>